server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # CRÍTICO: Si no encuentra el archivo o directorio, redirige a index.html
        try_files $uri $uri/ /index.html; 
    }
}